'use babel';

import UpdateDescriptorView from '../lib/update-descriptor-view';

describe('UpdateDescriptorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
